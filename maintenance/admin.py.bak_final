# --- SAFETY: force default admin namespace ---
try:
    if getattr(admin.site, "name", None) != "admin":
        admin.site.name = "admin"
except Exception:
    pass
from django.contrib import admin
from .models import Equipment

@admin.register(Equipment)
class EquipmentAdmin(admin.ModelAdmin):
    list_display = ("id", "name", "serial_number")  # sadece kesin olanlar
    search_fields = ("name", "serial_number")
    # GeÃ§ici olarak filtre yok (hata Ã§Ä±karanlarÄ± kaldÄ±rdÄ±k)
    # list_filter = ()
    def has_add_permission(self, request):
        return True
