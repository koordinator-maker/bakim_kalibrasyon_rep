# Admin > Equipment (triage; dayanıklı)
GOTO /admin/login/
WAIT SELECTOR input#id_username
FILL input#id_username admin
FILL input#id_password Admin!2345
CLICK input[type=submit]
WAIT URL CONTAINS /admin/
WAIT SELECTOR #content-main

# 1) Önce tıklayarak dene (link text'i 'Equipment' olan <a>)
CLICK a:has-text("Equipment")
# Bazı durumlarda Chrome error sayfasına düşerse yine de ekran alalım
WAIT SELECTOR body
SCREENSHOT targets/screens/admin-equipment-click.png

# 2) Fallback: doğrudan standart changelist URL’sine git
GOTO /admin/maintenance/equipment/
WAIT SELECTOR h1
SCREENSHOT targets/screens/admin-equipment.png

# Nihai beklenti: doğru rota (başarısız olursa sadece bu flow fail olacak;
# NonBlocking mod pipeline'ı düşürmez)
EXPECT URL CONTAINS /admin/maintenance/equipment/
