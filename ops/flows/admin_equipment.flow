# Admin > Equipment (triage)
GOTO /admin/login/
WAIT SELECTOR input#id_username
FILL input#id_username admin
FILL input#id_password Admin!2345
CLICK input[type=submit]
WAIT URL CONTAINS /admin/
WAIT SELECTOR #content-main

# Admin ana sayfadan Equipment linkine tıkla (direkt GOTO yerine)
CLICK text=Equipment
WAIT SELECTOR h1
SCREENSHOT targets/screens/admin-equipment.png

# Yalnızca raporda görünsün diye beklenti (başarısız olursa flow failed olur; NonBlocking pipeline kırmayacak)
EXPECT URL CONTAINS /admin/maintenance/equipment/
