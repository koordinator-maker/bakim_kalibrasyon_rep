# Admin -> Plan sayfası (metin regex ile)
GOTO /admin/login/
WAIT SELECTOR input#id_username
FILL input#id_username admin
FILL input#id_password Admin!2345
CLICK input[type=submit]
WAIT URL CONTAINS /admin/
WAIT SELECTOR #content-main

# "Plan", "Planlar", "Maintenance Plan" vb. yakalamak için regex:
WAIT SELECTOR text=/plan(lar|ları)?|maintenance\s*plan/i
CLICK text=/plan(lar|ları)?|maintenance\s*plan/i

WAIT SELECTOR h1
SCREENSHOT targets/screens/admin-plan.png
# Başlık termi değişkense bu EXPECT'i geçici kaldırabilirsin
