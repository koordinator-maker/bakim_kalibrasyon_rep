# Admin -> Calibrations (doğrudan URL)
GOTO /admin/login/
WAIT SELECTOR input#id_username
FILL input#id_username admin
FILL input#id_password Admin!2345
CLICK input[type=submit]
WAIT URL CONTAINS /admin/
WAIT SELECTOR #content-main

# hedef sayfa
GOTO /admin/maintenance/calibrations/
WAIT SELECTOR h1
SCREENSHOT targets/screens/admin-calibrations.png
EXPECT URL CONTAINS /admin/maintenance/calibrations/
